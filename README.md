# 🎂 Age to 100 - Python Program

یک برنامه پایتون که نام و سن کاربر را دریافت می‌کند و محاسبه می‌کند چند سال دیگر کاربر ۱۰۰ ساله می‌شود.

---

## 📌 توضیح عملکرد

این برنامه:

- نام کاربر را دریافت می‌کند
- سن کاربر را دریافت می‌کند
- تعداد سال‌های باقی‌مانده تا ۱۰۰ سالگی را محاسبه می‌کند
- نتیجه را نمایش می‌دهد

---


## ▶️ نحوه اجرا

ابتدا مطمئن شوید Python 3 روی سیستم شما نصب است.

در ترمینال یا PowerShell دستور زیر را اجرا کنید:

```bash
python main.py
```

یا در ویندوز:

```bash
py main.py
```


---

## 📂 پیش‌نیازها

- Python 3.x

---

## 🎯 هدف پروژه

این پروژه برای تمرین مفاهیم پایه زیر طراحی شده است:

- دریافت ورودی از کاربر (`input`)
- تبدیل نوع داده (`int`)
- انجام محاسبه ریاضی ساده
- نمایش خروجی (`print`)

---
